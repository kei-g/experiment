jobs:
  invoke:
    name: Invoke Job
    runs-on: ubuntu-latest
    steps:
      - name: Invoke the action to show env
        uses: kei-g/experiment/env@main
        with:
          value: ${{ vars.FOO }}
name: Env
on:
  issue_comment:
    types:
      - created
      - edited
  pull_request_review_comment:
    types:
      - created
      - edited
  pull_request_target:
    types:
      - opened
      - reopened
